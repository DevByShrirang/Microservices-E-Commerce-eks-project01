apiVersion: v1
kind: Service
metadata:
  name: {{ include "microservice-project.fullname" . }}
  labels:
    {{- include "microservice-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microservice-project.selectorLabels" . | nindent 4 }}
